# Staging build (`vite build --mode staging`). Committed on purpose: this is a
# public URL, not a credential, and an uncommitted copy would mean a staging
# build silently talking to the production API — the exact failure this split
# exists to prevent.
VITE_API_URL=https://nexus-api-staging.mingzhe030228.workers.dev
